/**
 * Sylvester E2E: App Lifecycle UAT Plan
 *
 * Stub — the original was in canary/checks/app-lifecycle.ts.
 * This exports the plan shape expected by index.ts.
 */
export const APP_LIFECYCLE_PLAN = {
  name: "app-lifecycle",
  description: "Full app lifecycle: create org → create app → deploy → verify → teardown",
  steps: [
    { id: "placeholder", description: "Sylvester app-lifecycle plan (stub)" },
  ],
};
